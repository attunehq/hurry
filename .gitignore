.env
# .scratch - temporarily un-ignored to share WIP state (re-ignore before merging)
.vscode
.hurrydata
target

# Un-ignore .claude directory to share commands with team
!.claude
# But ignore local settings
.claude/settings.local.json
